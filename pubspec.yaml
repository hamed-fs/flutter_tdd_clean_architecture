name: flutter_tdd_clean_architecture
description: Flutter TDD Clean Architecture project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # Service Locator
  get_it: ^4.0.2
  # Bloc for State Management
  flutter_bloc: ^4.0.1
  # Value Equality
  equatable: ^1.2.0
  # Functional Programming
  dartz: ^0.9.1
  # Remote API
  connectivity: ^0.4.8+6
  http: ^0.12.1
  # Locale Cache
  shared_preferences: ^0.5.7+3

  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.1

flutter:
  uses-material-design: true
